# Required: get a key at https://console.typesafe.ai/keys
TYPESAFE_API_KEY=

# Optional usage limits (defaults shown)
# SESSION_MINUTES=10
# IP_MINUTE_LIMIT=30
# IP_DAILY_LIMIT=300
# SESSION_SECRET=            # defaults to the API key

# Optional: Upstash Redis for the IP rate limit on Vercel (added automatically by the Vercel integration)
# KV_REST_API_URL=
# KV_REST_API_TOKEN=
